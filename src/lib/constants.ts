/**
 * Shared UI Constants
 * Common constants used across components
 */

/**
 * Opacity classes for out-of-scale or disabled elements
 */
export const UI_OPACITY = {
	/** Reduced opacity for non-scale elements when scale filter is active */
	OUT_OF_SCALE: 'opacity-40'
} as const;
